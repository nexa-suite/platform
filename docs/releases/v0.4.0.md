# Nexa Platform v0.4.0

## Historical version mapping

Former historical label: `v0.3.0`. Public version: `v0.4.0`. Target commit,
original chronology and release evidence were preserved during the
Owner-authorized SCM normalization on 2026-09-01.

## Scope

Angular 22 internal operations shell at `/overview`, packaged as a static Nginx image for the modern runtime.

## Added

- Dockerfile, SPA fallback, healthcheck and Compose port `4200`.
- 50 canonical catalog media assets with checksum validation.
- EN/ES shell and focused unit tests.

## Validation

- `npm run validate:catalog-assets`: 50 assets passed.
- `npm test`: 25 tests passed.
- `npm run build`: passed.
- Playwright smoke at `/overview`: served `200`.

## Boundary

No API client, authentication, tenant authorization, persistence or visual parity certification against matched Vue captures is included.
